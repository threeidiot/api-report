sudo: false

language: php

php:
  - 7.1
  - 7.2

install:
  - composer validate -d api
  - composer install -d api

script:
  # config laravel env
  - cp api/.env.example cp api/.env
  - php api/artisan key:generate
  # Unit test
  - ./api/vendor/bin/phpunit -c api/phpunit.xml

